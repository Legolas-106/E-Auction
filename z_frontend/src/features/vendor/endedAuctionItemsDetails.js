import React, { useState } from "react";

const EndedAuctionItemsDetails = ({data}) =>{
    const [showItemDetails,setShowItemDetails] = useState(false);
    
}

export default EndedAuctionItemsDetails;